# t3307-notes-man

- Claimed `t3307-notes-man` from `plan.md`.
- Read `AGENTS.md`, the `plan.md` entry, `git/t/t3307-notes-man.sh`, and `git/Documentation/git-notes.adoc`.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t3307 bash scripts/run-upstream-tests.sh t3307 2>&1 | tail -40`.
- Observed the upstream test already passed at `3/3` against the current `target/release/grit`.
- Inspected `grit/src/commands/notes.rs` to confirm the implemented `git notes add`, `copy`, and `show` behavior matched the tested manpage examples.
- Rebuilt the binary from source with `cargo build --release -p grit-rs`.
- Re-ran `CARGO_TARGET_DIR=/tmp/grit-build-t3307 bash scripts/run-upstream-tests.sh t3307 2>&1 | tail -40` and confirmed `3/3` still passed.
- Updated `plan.md` and `progress.md` to reflect that `t3307-notes-man` is complete.
- No Rust source changes were required for this task; the plan entry was stale.
