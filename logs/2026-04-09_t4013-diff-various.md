# t4013-diff-various

## Summary

Made `tests/t4013-diff-various.sh` pass (230/230).

## Changes

- **log**: Implemented Git-style merge diff handling (`-m`, `-c`, `--dd`, `--diff-merges`, `log.diffMerges` validation, default off for merges unless `--first-parent` or explicit merge-diff flags). Separate merge mode repeats commit headers with `(from <parent>)` for parents after the first. Pickaxe/filter paths unchanged for non-remerge.
- **diff**: Added `-I` / `--ignore-matching-lines` (repeatable), invalid-regex exit 129 with Git message; combined with `--ignore-blank-lines` for stat/entry filtering per Git xdiff semantics; patch uses line filtering only.
- **main**: Preprocess `git diff --no-rename` → exit 129 + `invalid option: --no-rename` (Git ambiguous abbreviation).
- **reflog**: Extended synthetic `log::Args` with new merge-diff fields.

## Validation

- `./scripts/run-tests.sh t4013-diff-various.sh` → 230/230
- `cargo test -p grit-lib --lib` → pass
- `cargo check -p grit-rs` → pass
