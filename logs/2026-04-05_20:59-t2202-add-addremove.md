# t2202-add-addremove

- Claimed `t2202-add-addremove` from `plan.md` and read `git/t/t2202-add-addremove.sh` plus `git/Documentation/git-add.adoc`.
- Reproduced the failure and traced it to top-level argument parsing: `git --literal-pathspecs add --all` was rejected before `add` ran.
- Updated `grit/src/main.rs` so Git pathspec-mode global flags are accepted before subcommand dispatch and exported via the matching `GIT_*_PATHSPECS` environment variables.
- Rebuilt with `CARGO_TARGET_DIR=/tmp/grit-build-t2202 cargo build --release -p grit-rs`.
- Verified command behavior manually in isolated `/tmp` repositories for all three `t2202` cases:
  - `git --literal-pathspecs add --all` in setup
  - `git add --all` staging tracked modifications, removals, and new unignored files
  - plain `git add` staying a no-op without a pathspec
- Refreshed `target/release/grit` to point at the rebuilt `/tmp` binary so `scripts/run-upstream-tests.sh` executed the updated release binary in this sandbox.
- Confirmed upstream verification with `CARGO_TARGET_DIR=/tmp/grit-build-t2202 bash scripts/run-upstream-tests.sh t2202 2>&1 | tail -40`: 3/3 passing.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t2202 cargo fmt --all 2>/dev/null; true`.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t2202 cargo test -p grit-lib --lib`: 97/97 passing.
- Attempted `CARGO_TARGET_DIR=/tmp/grit-build-t2202 cargo clippy --fix --allow-dirty`, but Cargo locking failed in this sandbox with `failed to bind TCP listener to manage locking`.
