# t5532-fetch-proxy

## Summary

Made `t5532-fetch-proxy.sh` pass (5/5).

## Changes

1. **`grit/src/fetch_transport.rs`**
   - `GIT_PROXY_COMMAND` and `core.gitproxy` (including `… for host` suffix rules and `none`) for `git://` fetches.
   - Reject host/port starting with `-` before spawning proxy (Git `looks_like_command_line_option`).
   - Spawn proxy with argv: `proxy-cmd host port`; stdin/stdout as git wire; `current_dir` = work tree so `./proxy` works with `GIT_DIR` set.
   - Direct TCP path unchanged when no proxy applies.

2. **`grit/src/commands/fetch.rs`**
   - Stop delegating `git://` to system git; use native transport + config.
   - FETCH_HEAD: when all positive fetch refspecs are globs (default `refs/heads/*:refs/remotes/<name>/*`), mark for-merge using checked-out branch on remote (same as empty refspec case) so `git log FETCH_HEAD` works.

3. **`grit/src/commands/clone.rs`**
   - Pass clone target dir as proxy `current_dir` for `git://` clone.

4. **`scripts/run-tests.sh`**
   - Export absolute `PERL_PATH` (`type -P perl` or `/usr/bin/perl`) so `write_script` shebangs are execve-valid (bare `perl` in shebang fails on Linux).

5. **`PLAN.md`**, **`progress.md`** — marked task complete; refreshed counts.

## Verification

- `./scripts/run-tests.sh t5532-fetch-proxy.sh` — 5/5
- `cargo test -p grit-lib --lib` — pass
