## Task: t4010-diff-pathspec

### Claim
- Claimed after completing `t4001-diff-rename`.
- Marked as `[~]` in `PLAN.md`.

### Baseline
- Tracked harness:
  - `./scripts/run-tests.sh t4010-diff-pathspec.sh`
  - result: **11/17 passing** (6 failing)
- Direct local run from `tests/`:
  - `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash t4010-diff-pathspec.sh`
  - result: **11/17 passing** (same 6 failing tests)

### Current failing assertions
- 7 `limit to file0/ should emit nothing.`
- 9 `diff-tree with wildcard shows dir also matches`
- 10 `diff-tree -r with wildcard`
- 11 `diff-tree with wildcard shows dir also matches`
- 12 `diff-tree -r with wildcard from beginning`
- 13 `diff-tree -r with wildcard`

### Initial observations
- Pathspec trailing-slash handling is too permissive for non-directory paths (`file0/` still matches `file0`).
- `diff-tree` wildcard pathspec handling is incomplete:
  - root tree resolution via `$EMPTY_TREE` (`4b825d...`) is failing with object lookup errors,
  - recursive/non-recursive wildcard matching behavior diverges from expected Git semantics.
- Remaining tests after these assertions (submodule + multi-pathspec) currently pass.
