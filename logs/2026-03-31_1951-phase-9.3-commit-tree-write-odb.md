## Phase 9.3 — commit-tree writes commit object and prints OID

- Scope: implement only `plan.md` item `9.3`.
- Verification in a fresh temporary repository:
  - Created repository, added one file, wrote tree with `gust write-tree`.
  - Ran `gust commit-tree <tree>` with message from stdin.
  - Confirmed `commit-tree` stdout value is a valid object id.
  - Confirmed object exists in ODB and is typed as `commit` via `gust cat-file -t <oid>`.
- Code status:
  - `gust commit-tree` already writes the serialized commit to ODB (`repo.odb.write(ObjectKind::Commit, &raw)`).
  - `gust commit-tree` prints the resulting commit hash to stdout.
- Outcome: no command code changes were required for `9.3`; behavior is verified and task is complete.
