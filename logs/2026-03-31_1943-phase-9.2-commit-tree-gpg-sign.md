## Phase 9.2 — commit-tree GPG signing (`-S`)

- Scope: implement only `plan.md` item `9.2`.
- Decision for v1: omit GPG signing support unless ported tests require it.
- Verification performed:
  - `gust commit-tree -S` returns a clap parse error (`unexpected argument '-S' found`).
  - `gust commit-tree --gpg-sign` returns a clap parse error (`unexpected argument '--gpg-sign' found`).
- Outcome: no code change required; existing behavior is consistent with the v1 omission decision (explicit reject, not silent ignore).
