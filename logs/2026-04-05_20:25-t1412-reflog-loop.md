# t1412-reflog-loop

- Claimed `t1412-reflog-loop` from `plan.md` and read `AGENTS.md`, `progress.md`, `git/t/t1412-reflog-loop.sh`, and the reflog-related command code.
- Reproduced the failure with `CARGO_TARGET_DIR=/tmp/grit-build-t1412 bash scripts/run-upstream-tests.sh t1412`, which reported 2/3 passing because `git log -g --format="%gd %gs" topic` was missing the oldest `topic@{4}` entry.
- Traced the issue to `grit/src/commands/checkout.rs`: `checkout -b` created `refs/heads/topic` but did not append the branch's creation reflog entry, so the branch reflog only contained the four later reset entries.
- Updated `checkout -b` to append `branch: Created from HEAD` (or the explicit start point) to the new branch reflog via `append_reflog`, while keeping the existing HEAD checkout reflog behavior intact.
- Rebuilt `target/release/grit` with `cargo build --release -p grit-rs`.
- Re-ran `CARGO_TARGET_DIR=/tmp/grit-build-t1412 bash scripts/run-upstream-tests.sh t1412` and confirmed 3/3 passing.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t1412 cargo fmt --all`.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t1412 cargo test -p grit-lib --lib` and confirmed 97/97 passing.
- Updated `plan.md`, `progress.md`, and `test-results.md` to reflect the completed task and current counts.
