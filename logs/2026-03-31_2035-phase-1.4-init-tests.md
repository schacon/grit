## Phase 1.4 - t0001-init port and pass

- Expanded `tests/t0001-init.sh` from a minimal 2-case subset to a broader init-focused subset.
- Added compatibility helpers in the test script to validate repo config directly from files, avoiding porcelain `git config` dependency.
- Added shared-repo coverage in the ported test:
  - explicit `--shared=0666` writes `core.sharedRepository`
  - global `core.sharedRepository` in `$HOME/.gitconfig` is honored
- Updated `grit init` command logic to read global `core.sharedRepository` when `--shared` is not provided.
- Verified selected harness test passes with all included cases green.
