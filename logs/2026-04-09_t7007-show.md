# t7007-show

## Outcome

All 18 tests in `tests/t7007-show.sh` pass after `cargo build --release -p grit-rs`.

## Changes (high level)

- `show`: rev-list walks, tree listing format, `-n`, `--merge`, `--graph` error, `%s` spacing for `-s`.
- `main`: skip post-subcommand `-C` stripping for `switch`/`checkout`.
- `checkout`: peel `-q` from rest; empty-index force-create; worktree conflict `we_are_main` fix.
- `merge`: resolve merge target to `refs/heads/<name>` when present.
- `tag`: existence check via `resolve_ref`.
- `rev_parse`: export `peel_to_tree`.

## Push

`git push -u origin cursor/t7007-show-test-suite-4bbb` failed (origin not a valid remote in this environment).
