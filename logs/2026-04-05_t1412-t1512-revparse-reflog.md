## 2026-04-05 — t1412-reflog-loop + t1512-rev-parse-disambiguation

### Scope
- `t1412-reflog-loop`
- `t1512-rev-parse-disambiguation`
- regression coverage for `t1407-worktree-ref-store`

### Changes
- Added `test-tool` compatibility handlers in `grit/src/main.rs`:
  - bare `test-tool` hashing mode (`sha1` stdin digest output)
  - `test-tool zlib deflate`
- Added loose-object prefix listing API in `grit-lib/src/rev_parse.rs`:
  - `list_loose_abbrev_matches(repo, prefix)`
- Improved rev-parse ambiguous short OID diagnostics in `grit/src/commands/rev_parse.rs`:
  - emit upstream-style `error/hint/fatal` output including candidate list
  - classify bad loose objects as `[bad object]`
  - emit `fatal: invalid object type` when all candidates are bad
  - return deterministic exit behavior expected by tests
- Restored upstream-compatible helpers in `tests/test-lib.sh`:
  - `test_oid_init` + `test_oid_to_path`
  - proper `test_commit --append` semantics (`>>` when requested)
- Updated checkout branch creation reflog behavior in `grit/src/commands/checkout.rs`:
  - `checkout -b` / `checkout -B` now append
    `branch: Created from <start>` when creating a new branch
  - switched zero OID fallback to `grit_lib::diff::zero_oid()`

### Validation
- `cargo fmt`
- `cargo build --release -p grit-rs`
- `GUST_BIN=/workspace/tests/grit TEST_VERBOSE=1 bash tests/t1512-rev-parse-disambiguation.sh` → **3/3**
- `./scripts/run-tests.sh t1512-rev-parse-disambiguation.sh` → **3/3**
- `GUST_BIN=/workspace/tests/grit TEST_VERBOSE=1 bash tests/t1412-reflog-loop.sh` → **3/3**
- `./scripts/run-tests.sh t1412-reflog-loop.sh` → **3/3**
- `./scripts/run-tests.sh t1407-worktree-ref-store.sh` → **4/4** (regression)
