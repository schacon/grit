## 2026-04-06 — t6060-merge-index

### Scope
- Claiming `t6060-merge-index` from plan as the next highest-priority remaining `t6*` item.

### Initial actions
- Claimed in `PLAN.md` as in progress (`[~]`).
- Updated `progress.md` counts accordingly.
- Next: reproduce failures with direct + harness runs, then implement merge-index/merge-one-file behavior gaps in Rust command layer.

### Reproduction summary
- Direct run (`GUST_BIN=/workspace/target/release/grit bash tests/t6060-merge-index.sh`) initially failed 4/7 with:
  - merge program invocation mismatch (`git-merge-one-file` compatibility),
  - missing worktree behavior mismatch in bare repo,
  - `cat-file :file` failure under `GIT_INDEX_FILE`,
  - `diff-files --diff-filter=U` not honoring filter spec (reported `M` path as unmerged list).

### Implemented fixes
- `grit/src/commands/merge_index.rs`
  - Added support for `-o` and `-q` flags.
  - Aligned merge-program argument order with Git:  
    `<base-oid> <ours-oid> <theirs-oid> <path> <base-mode> <ours-mode> <theirs-mode>`.
  - Added compatibility dispatch for `git-merge-one-file` to internal `grit merge-one-file`.
  - Ensured index loading respects `GIT_INDEX_FILE`.
- `grit/src/commands/merge_one_file.rs`
  - Reworked helper argument parsing to Git-compatible order.
  - Enforced worktree requirement (`this operation must be run in a work tree`) for bare-without-worktree case.
  - Implemented clean three-way content merge via `grit_lib::merge_file`.
  - Updated index stage resolution by replacing stage 1/2/3 entries with a stage-0 merged entry.
  - Ensured index read/write respects `GIT_INDEX_FILE`.
- `grit/src/commands/diff_files.rs`
  - Implemented actual `--diff-filter` parsing and filtering (including separate `--diff-filter <spec>` and `--diff-filter=<spec>`).
  - Added include/exclude letter matching needed for `--diff-filter=U`.
- `grit-lib/src/rev_parse.rs`
  - Updated `:path` / `:N:path` index lookups to honor `GIT_INDEX_FILE`, fixing `cat-file :file` in alternate-index scenarios.

### Validation
- Direct:
  - `GUST_BIN=/workspace/target/release/grit bash tests/t6060-merge-index.sh` → **7/7 pass**.
- Harness:
  - `./scripts/run-tests.sh t6060-merge-index.sh` → **7/7 pass**.
- Regressions:
  - `./scripts/run-tests.sh t6427-diff3-conflict-markers.sh` → 9/9 pass.
  - `./scripts/run-tests.sh t6404-recursive-merge.sh` → 6/6 pass.

### Result
- `t6060-merge-index` is fully passing and ready to mark done in plan/progress tracking.
