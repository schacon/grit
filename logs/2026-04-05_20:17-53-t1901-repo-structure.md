# t1901-repo-structure

- Read `AGENTS.md`, `plan.md`, and `git/t/t1901-repo-structure.sh`.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t1901 bash scripts/run-upstream-tests.sh t1901 2>&1 | tail -40`; the starting summary still referenced the stale `2/4` state.
- Re-ran `CARGO_TARGET_DIR=/tmp/grit-build-t1901 bash scripts/run-upstream-tests.sh t1901` and inspected `/tmp/grit-upstream-results/t1901-repo-structure.out`.
- Found the remaining mismatch in the populated-repository table output: `repo structure` used float-based humanization, but upstream Git uses integer arithmetic with specific rounding thresholds.
- Updated `grit/src/commands/repo.rs` so `humanize_count()` and `humanize_bytes()` follow Git’s `strbuf.c` logic. This fixed the `217.92 KiB` vs `217.93 KiB` table mismatch.
- Built `target/release/grit` and reproduced the populated repository both with system Git setup and with Grit’s own setup commands; both matched the expected `t1901` table after the formatter fix.
- `scripts/run-upstream-tests.sh t1901` still emitted a stale `3/4` summary in this sandbox, so I ran the upstream shell test in an isolated manual harness (`/tmp/run_t1901_manual.sh`) and verified `t1901-repo-structure.sh` passes `4/4`.
- Validation:
  - `cargo fmt`
  - `bash /tmp/run_t1901_manual.sh`
  - Result: `t1901-repo-structure` passes `4/4`
- `CARGO_TARGET_DIR=/tmp/grit-build-t1901 cargo clippy --fix --allow-dirty` could not complete in this sandbox because Cargo fix failed with `failed to bind TCP listener to manage locking` (`Operation not permitted`).
