## Task: t4125-apply-ws-fuzz

### Claim
- Claimed after completing `t4007-rename-3`.
- Marked as `[~]` in `PLAN.md`.

### Baseline
- `./scripts/run-tests.sh t4125-apply-ws-fuzz.sh` reports `2/4` passing (`2` left).

### Next
- Reproduce the two failing assertions from `tests/t4125-apply-ws-fuzz.sh`.
- Compare current `grit apply` behavior with expected whitespace-fuzz context handling.
- Implement and validate fixes until `4/4` passes.
