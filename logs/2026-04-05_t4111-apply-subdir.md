# t4111-apply-subdir

- Date: 2026-04-05
- Scope: verify and close out stale `plan.md` coverage entry for upstream `git/t/t4111-apply-subdir.sh`

## Actions

- Read `AGENTS.md`, `plan.md`, `git/t/t4111-apply-subdir.sh`, and `git/Documentation/git-apply.adoc`.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t4111 bash scripts/run-upstream-tests.sh t4111-apply-subdir 2>&1 | tail -40`.
- Observed the current `target/release/grit` already passes the full upstream file: 10 tests, 10 passing, 0 failing.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t4111 cargo fmt --all 2>/dev/null; true`.
- Updated `plan.md`, `progress.md`, and `test-results.md` to reflect the verified green state.

## Notes

- No Rust source changes were required for this task.
- There was an unrelated pre-existing modification in `grit/src/commands/rebase.rs`; it was left untouched.
