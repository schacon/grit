## t4119-apply-config

### Claim / baseline
- Marked `t4119-apply-config` as in-progress (`[~]`) in `PLAN.md`.
- Local baseline: `./scripts/run-tests.sh t4119-apply-config.sh` → **1/11**.
- Upstream baseline: `bash scripts/run-upstream-tests.sh t4119-apply-config` → **1/11**.

### Initial notes
- Focus area: `git apply --whitespace=strip` behavior and config interactions.
- Next step: inspect failing assertions from upstream `.out` and align whitespace/config handling in `grit/src/commands/apply.rs`.
