# 2026-04-06 — t1406-submodule-ref-store + t1405-main-ref-store

## Scope
- Target files:
  - `tests/t1406-submodule-ref-store.sh`
  - `tests/t1405-main-ref-store.sh`
- Start state:
  - `t1406`: 8/15 passing
  - `t1405`: 2/16 passing
- Goal: implement missing `test-tool ref-store` helper surface in `grit/src/main.rs` without modifying upstream tests.

## Root cause
`test-tool ref-store` in `grit/src/main.rs` only implemented `worktree:<id>` backend with two subcommands (`resolve-ref`, `create-symref`).  
Upstream tests for main/submodule ref stores require a much broader command surface plus backend-specific read-only/write behavior.

## Implemented fixes

### `grit/src/main.rs`
- Expanded ref-store usage/dispatch to support:
  - backends: `main`, `submodule:<path>`, `worktree:<id>`
  - backend traits: writable (`main`,`worktree`) vs read-only (`submodule`)
- Added helper model:
  - `TestToolRefStoreBackendKind`
  - `TestToolRefStoreBackend`
  - backend resolvers for worktree/submodule git dirs
- Added flag parsing for ref-store helpers:
  - `REF_NO_DEREF`
  - tolerant parsing of other known transaction flag names used by tests
- Implemented/extended subcommands:
  - `resolve-ref` (with flags and `(null)` output on failure)
  - `create-symref`
  - `delete-refs`
  - `rename-ref` (including reflog file rename)
  - `for-each-ref`
  - `for-each-ref--exclude`
  - `verify-ref` (prefix D/F conflict detection semantics used by tests)
  - `for-each-reflog`
  - `for-each-reflog-ent`
  - `for-each-reflog-ent-reverse`
  - `reflog-exists`
  - `delete-reflog`
  - `create-reflog`
  - `delete-ref`
  - `update-ref` (with old-oid validation + reflog append)
  - `pack-refs` (explicit failure path for currently covered tests)
- Enforced read-only failures for submodule backend write operations (`create-symref`, `delete-refs`, `rename-ref`, `delete-reflog`, `create-reflog`, etc.).

## Validation
- `cargo build --release -p grit-rs` ✅
- `rm -rf /workspace/tests/trash.t1406-submodule-ref-store && GUST_BIN=/workspace/target/release/grit TEST_VERBOSE=1 bash t1406-submodule-ref-store.sh` ✅ `15/15`
- `rm -rf /workspace/tests/trash.t1405-main-ref-store && GUST_BIN=/workspace/target/release/grit TEST_VERBOSE=1 bash t1405-main-ref-store.sh` ✅ `16/16`
- `./scripts/run-tests.sh t1406-submodule-ref-store.sh` ✅ `15/15`
- `./scripts/run-tests.sh t1405-main-ref-store.sh` ✅ `16/16`
- `./scripts/run-tests.sh t1407-worktree-ref-store.sh` ✅ `4/4` (regression)
- `cargo fmt` ✅
- `cargo clippy --fix --allow-dirty` ✅ (reverted unrelated non-target edits)
- `cargo test -p grit-lib --lib` ✅ `98/98`
- `cargo check -p grit-rs` ✅

## Tracking updates
- `PLAN.md`:
  - marked `t1406-submodule-ref-store` as complete (`15/15`)
  - marked `t1405-main-ref-store` as complete (`16/16`)
  - updated total summary: `8,734/24,806 tests passing, 16,072 failures remaining`
- `progress.md`:
  - updated counts to `Completed 85`, `Remaining 682`, `Total 767`
  - added recent-completion notes for `t1406` and `t1405`
- `test-results.md`:
  - prepended build/test evidence for this run
