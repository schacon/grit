## Phase 8.2 — checkout-index flags subset

- Implemented `checkout-index` option behavior for `-a`, `--force`, `-u`, `-q`, `-n`/`--no-create`, `-z` path input with `--stdin`, `--prefix`, and `--stage=all` stage selection parsing.
- Added argument conflict checks to match upstream constraints for mixing `--all`, `--stdin`, and explicit path arguments.
- Wired `-u` to refresh index stat fields for checked-out entries and persist index updates when applicable.
- Updated `plan.md` to mark `8.2` as complete.

## Verification

- `cargo fmt`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
