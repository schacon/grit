## Phase 0.1 Cargo workspace

- Verified workspace layout: root `Cargo.toml` defines members `grit` (binary) and `grit-lib` (library).
- Verified dependency split per contract:
  - `grit` uses `anyhow`.
  - `grit-lib` uses `thiserror`.
- Verified workspace lints deny `clippy::unwrap_used` and `clippy::expect_used`.
- Added test-only lint allowances in `grit-lib` test modules so production paths remain denied while `--all-targets` clippy passes:
  - `grit-lib/src/index.rs`
  - `grit-lib/src/odb.rs`
- Fixed two pre-existing clippy warnings in `grit` command code (no feature scope changes):
  - simplified identical branch output in `grit/src/commands/init.rs`
  - simplified identical branch size formatting in `grit/src/commands/ls_tree.rs`

## Validation commands run (repo root)

`cargo fmt`

`cargo clippy --workspace --all-targets -- -D warnings`

`cargo build --workspace`

`cargo test --workspace`

All commands completed successfully.
