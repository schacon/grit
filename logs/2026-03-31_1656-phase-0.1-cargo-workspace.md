## Phase 0.1 Cargo workspace

- Verified workspace layout: root `Cargo.toml` defines members `gust` (binary) and `gust-lib` (library).
- Verified dependency split per contract:
  - `gust` uses `anyhow`.
  - `gust-lib` uses `thiserror`.
- Verified workspace lints deny `clippy::unwrap_used` and `clippy::expect_used`.
- Added test-only lint allowances in `gust-lib` test modules so production paths remain denied while `--all-targets` clippy passes:
  - `gust-lib/src/index.rs`
  - `gust-lib/src/odb.rs`
- Fixed two pre-existing clippy warnings in `gust` command code (no feature scope changes):
  - simplified identical branch output in `gust/src/commands/init.rs`
  - simplified identical branch size formatting in `gust/src/commands/ls_tree.rs`

## Validation commands run (repo root)

`cargo fmt`

`cargo clippy --workspace --all-targets -- -D warnings`

`cargo build --workspace`

`cargo test --workspace`

All commands completed successfully.
