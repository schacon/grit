# t7001-mv

- Fixed `git mv` for submodules: collision check before staging submodule ops (failed `mv sub file` no longer dirties `.gitmodules`), nested `modules/<seg>/modules/...` gitdir moves via `submodule_modules_git_dir`, `.gitmodules` staging guard + missing-section warning, `fatal:` parent/child messages.
- Checkout: superproject gitlink removal uses `rmdir` + `warning: unable to rmdir` when non-empty (t7001 test 49).
- `git status -s path`: `preprocess_status_argv` inserts `--` before pathspecs.
- Submodule: `submodule add` resolves relative URLs from cwd; `submodule init` and `submodule update` clone use filesystem-resolved URLs (`submodule_clone_argument`).

Harness: `./scripts/run-tests.sh t7001-mv.sh` → 54/54.
