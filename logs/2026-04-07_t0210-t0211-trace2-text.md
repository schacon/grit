## t0210 + t0211 trace2 text targets — 2026-04-07

### Goal
Make `t0210-trace2-normal.sh` and `t0211-trace2-perf.sh` fully pass.

### Baseline
- `./scripts/run-tests.sh t0210-trace2-normal.sh` → **13/14** (FAIL 11 valueless config variable output)
- `./scripts/run-tests.sh t0211-trace2-perf.sh` → **14/17** (remaining alias expansion/def_param hierarchy failures)

### Implementation notes
- Refined trace2 text writer behavior in `grit/src/main.rs`:
  - added process-depth propagation for child processes via `GIT_TRACE2_PARENT_DEPTH`,
  - split process-depth (`dN`) from command hierarchy for alias unpeeling,
  - corrected alias cmd_name hierarchy progression for:
    - `_run_dashed_`,
    - `_run_git_alias_`,
    - `_run_shell_alias_`,
    - nested alias chains (`alias.xxx -> yyy -> version`),
  - fixed nested-alias recursion to avoid duplicate `_run_dashed_` hierarchy segment,
  - emitted alias events with proper argv payload (pre-pop first token).
- Restored trace2 `cmd_ancestry` output compatibility after refactor:
  - normal target emits `a <- b <- c` (without `ancestry:[...]` wrapper),
  - perf target emits `ancestry:[a b c]`,
  - event target emits JSON `ancestry` array,
  - drops current process name from ancestry payload to match upstream filters.
- Fixed bare command-line config semantics in `grit-lib/src/config.rs`:
  - introduced `add_command_override_bare()` storing `value: None`,
  - `GIT_CONFIG_PARAMETERS` bare entries now use valueless command overrides,
  - adjusted `parse_bool()` to treat only `true/yes/on` (and numeric non-zero) as true; empty string no longer parses as implicit true.

### Validation
- `cargo build --release -p grit-rs` ✅
- `GUST_BIN=/workspace/target/release/grit TEST_VERBOSE=1 bash t0211-trace2-perf.sh` ✅ **17/17**
- `GUST_BIN=/workspace/target/release/grit TEST_VERBOSE=1 bash t0210-trace2-normal.sh` ✅ **14/14**
- `GUST_BIN=/workspace/target/release/grit TEST_VERBOSE=1 bash t0213-trace2-ancestry.sh` ✅ **5/5** (regression check after ancestry formatting fix)
- Harness runs:
  - `./scripts/run-tests.sh t0210-trace2-normal.sh` ✅ **14/14**
  - `./scripts/run-tests.sh t0211-trace2-perf.sh` ✅ **17/17**
  - `./scripts/run-tests.sh t0212-trace2-event.sh` ✅ **11/11**
  - `./scripts/run-tests.sh t0213-trace2-ancestry.sh` ✅ **5/5**
- Quality gates:
  - `cargo fmt && cargo clippy --fix --allow-dirty && cargo test -p grit-lib --lib` ✅

### Result
- `t0210-trace2-normal` now fully passing.
- `t0211-trace2-perf` now fully passing.
