# t0091-bugreport

## Issue

Tests 6–7 failed: `test_grep usage output` expected lowercase `usage:` like Git; clap’s `Error::print()` for unknown arguments emitted `Usage:`.

## Fix

In `parse_cmd_args` (`grit/src/main.rs`), for non-help clap errors, render with `e.render()`, replace `Usage:` → `usage:`, and print to stderr (matching clap’s stream for errors).

## Verification

- `bash tests/t0091-bugreport.sh` — 13/13 passed (3 UNZIP tests skipped as expected).
- `./scripts/run-tests.sh t0091-bugreport.sh` — ✓ 13/13.
