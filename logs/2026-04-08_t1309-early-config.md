# 2026-04-08 — t1309-early-config (10/10)

## Changes

- **Ceiling discovery**: Replaced exact-match ceiling blocking with Git’s `longest_ancestor_length` + parent walk (signed offsets to avoid underflow loops).
- **`read_early_config`**: Matches Git’s early sequence — system, XDG then `~/.gitconfig`, repo `config` (skipped with warning when `core.repositoryformatversion` > 1), optional `config.worktree` when `extensions.worktreeConfig`, then `GIT_CONFIG_PARAMETERS` with include expansion. Works with `git_dir: None` when no repo (ceiling #2).
- **Global config order**: XDG before `~/.gitconfig` in `global_config_paths()` and `load_with_options` (merge all existing globals).
- **Discovery**: `open_skipping_format_validation` so unsupported format still yields a `Repository` with `git_dir` for early config; `try_open_at` uses it for `.git` dirs and gitfiles.
- **test-tool**: `read_early_config` prints all values in load order via `get_all`.
- **Tests**: `test_expect_success` for invalid repo version / invalid config (previously expected failure).

## Verification

- `./scripts/run-tests.sh t1309-early-config.sh` → 10/10
- `cargo test -p grit-lib --lib` → pass
- `cargo check -p grit-lib -p grit-rs` → pass
