## Task

Phase 7.1: implement `read-tree` single-tree read into empty or existing index (no merge).

## Changes

- Restricted `grit read-tree` to the Phase 7.1 scope:
  - rejects `-m` merge mode as not yet implemented,
  - rejects `-u`/`--reset` working tree update modes as not yet implemented,
  - rejects `--prefix` and `--aggressive` as not yet implemented,
  - requires exactly one tree-ish argument in non-merge mode.
- Implemented single-tree behavior to always rebuild the index from the resolved tree.
- Improved tree-ish resolution:
  - accepts tree OIDs directly,
  - resolves refs/branch names,
  - peels commit objects to their referenced tree.
- Updated `plan.md` to mark only item **7.1** as complete.

## Validation

- `cargo fmt`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`

All commands passed.
