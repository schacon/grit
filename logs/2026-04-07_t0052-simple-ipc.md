# t0052-simple-ipc

- Added `grit-lib::simple_ipc`: Unix domain socket server (accept + worker pool), pkt-line framing matching Git (`LARGE_PACKET_DATA_MAX` chunking), test daemon commands (`ping`, `big`, `chunk`, `slow`, `sendbytes`, `quit`).
- Wired `grit test-tool simple-ipc` and `tests/test-tool` dispatch to grit.
- Implemented Git-style `test_atexit` in `tests/test-lib.sh` + `test_done` hook in `tests/test-lib-tap.sh` so `t0052` cleanup runs (requires `bash` for `BASH_SUBSHELL` check).
- Harness: `./scripts/run-tests.sh --timeout 300 t0052-simple-ipc.sh` → 9/9 pass.
