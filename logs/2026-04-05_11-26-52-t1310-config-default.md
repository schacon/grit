# t1310-config-default

- Date: 2026-04-05 11:26:52 CEST
- Task: make `t1310-config-default` pass 5/5

## Summary

Patched `grit config` so `--default` is rejected for non-lookup operations and typed fallback values are formatted through a dedicated error path.

## Changes

- added early validation in `grit/src/commands/config.rs` so `--default` only works with `--get`, `--get-all`, `--get-regexp`, and single-key lookup forms
- added `format_default_value()` to report `failed to format default config value` on typed fallback failures
- added minimal `expiry-date` formatting support for fallback values so invalid defaults fail instead of being echoed back unchanged
- removed an unrelated `unused_mut` warning in `grit-lib/src/rev_list.rs` so `cargo check -p grit-rs` is warning-free

## Validation

- manual spot checks:
  - `grit config -f config --type=expiry-date --default=x --get not.a.section` now exits 1 and prints `failed to format default config value`
  - `grit config --default=quux --unset a.section` now exits 1 and prints `--default is only applicable to`
- upstream-style harness:
  - `cd tests && cp ../target/release/grit ./grit && chmod +x ./grit && GUST_BIN="$(pwd)/grit" bash ./t1310-config-default.sh`
  - result: `# Tests: 5  Pass: 5  Fail: 0  Skip: 0`

## Notes

- The requested `/Users/schacon/projects/grit/scripts/run-upstream-tests.sh t1310` command is stale in this environment and aborts immediately because it hardcodes `REPO=/home/hasi/grit`.
- `cargo fmt` ran successfully.
- `cargo clippy --fix --allow-dirty` could not complete in this sandbox because Cargo failed with `failed to bind TCP listener to manage locking` (`Operation not permitted`).
