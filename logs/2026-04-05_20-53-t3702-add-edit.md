# t3702-add-edit

- Claimed the `t3702-add-edit` task and reviewed `AGENTS.md`, `plan.md`, `git/t/t3702-add-edit.sh`, and the `git-add` documentation for `-e`.
- Reproduced the failure at `1/3` passing; `grit add` still treated `-e` as an unimplemented stub.
- Implemented `git add -e` in `grit/src/commands/add.rs` by generating the index-vs-worktree patch, launching the configured editor with shell semantics compatible with upstream `test_set_editor`, treating an empty edited patch as an abort/no-op, and applying the edited patch back to the index through the existing `apply --cached --recount` path.
- Added clean failure handling for non-zero editor exits so the command returns an error without mutating the index.
- Rebuilt `target/release/grit` and reran `CARGO_TARGET_DIR=/tmp/grit-build-t3702-add-edit bash scripts/run-upstream-tests.sh t3702-add-edit 2>&1 | tail -40`.
- Final upstream result: `3/3` passing for `t3702-add-edit`.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t3702-add-edit cargo fmt --all 2>/dev/null; true`.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t3702-add-edit cargo test -p grit-lib --lib` and got `97/97` passing.
- Attempted `CARGO_TARGET_DIR=/tmp/grit-build-t3702-add-edit cargo clippy --fix --allow-dirty`, but the sandbox blocked it with `failed to bind TCP listener to manage locking (Operation not permitted)`.
