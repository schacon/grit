# t3005-ls-files-relative

- Claimed `t3005-ls-files-relative` from `PLAN.md` at 1/4 passing and read `AGENTS.md`, `PLAN.md`, `git/t/t3005-ls-files-relative.sh`, and `git/Documentation/git-ls-files.adoc`.
- Reproduced the failure with `CARGO_TARGET_DIR=/tmp/grit-build-t3005 bash scripts/run-upstream-tests.sh t3005-ls-files-relative 2>&1 | tail -40`, then used manual scratch repos under `/tmp` to isolate the `ls-files` behavior from `top/sub`.
- Fixed `grit/src/commands/ls_files.rs` so tracked index paths are normalized before matching and display, subdirectory-relative output uses `../` segments correctly, glob pathspecs containing `..` resolve against the work tree, and `--error-unmatch` reports only paths that survive the selected output mode.
- Adjusted `grit/src/commands/pull.rs` to initialize newly added `merge::Args` and `rebase::Args` fields, and updated `grit/src/commands/rebase.rs` call sites so the existing worktree changes compile into `target/release/grit`.
- Rebuilt with `cargo build --release -p grit-rs`, reran `rm -rf /tmp/grit-upstream-workdir /tmp/grit-upstream-results && CARGO_TARGET_DIR=/tmp/grit-build-t3005 bash scripts/run-upstream-tests.sh t3005-ls-files-relative 2>&1 | tail -40`, and confirmed 4/4 passing.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t3005 cargo fmt --all 2>/dev/null; true`.
