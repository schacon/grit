## Phase 4.3 - ls-files

- Claimed and completed `plan.md` item `4.3`.
- Reworked `grit/src/commands/ls_files.rs` to support:
  - Default cached listing with multi-stage entries.
  - `-s`, `-u`, `-o`, `-i`, `-m`, `-d`, `-k`, `-z`.
  - `--exclude`, `--exclude-from`, pathspec filtering.
  - `--format` interpolation (`objectmode`, `objectname`, `objecttype`, `objectsize`, `stage`, `path`).
  - `--deduplicate`, `--error-unmatch`, and `--with-tree`.
- Added argument validation for incompatible mode combinations.
- Added tree-ish resolution including simple `~N` parent traversal for `--with-tree`.
- Next steps in this loop: run `cargo fmt`, strict `clippy`, and workspace tests; then commit with GitButler.
