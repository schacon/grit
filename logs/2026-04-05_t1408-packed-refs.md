# t1408-packed-refs

- Date: 2026-04-05
- Scope: verify and close out `t1408-packed-refs`

## Notes

- Read `AGENTS.md`, `PLAN.md`, and `git/t/t1408-packed-refs.sh`.
- Reproduced with `CARGO_TARGET_DIR=/tmp/grit-build-t1408 bash scripts/run-upstream-tests.sh t1408 2>&1 | tail -40`.
- Found the test already passed 3/3 in the current source tree; the stale state was in `PLAN.md`, not in the Rust implementation.
- Rebuilt the release binary with `cargo build --release -p grit-rs` because `scripts/run-upstream-tests.sh` executes `target/release/grit` directly.
- Re-ran `t1408`; confirmed 3/3 passing against the rebuilt binary.
- `./scripts/run-tests.sh t1408-packed-refs.sh` — **3/3 passing**.
- Ran `cargo fmt`.
- Attempted `CARGO_TARGET_DIR=/tmp/grit-build-t1408 cargo clippy --fix --allow-dirty`; sandbox blocked Cargo's lock manager with `failed to bind TCP listener to manage locking`.
- Updated `PLAN.md`, `progress.md`, and `test-results.md` to reflect the verified state.

## Outcome

- `t1408-packed-refs` is fully passing.
- No Rust source change was required for this task.
