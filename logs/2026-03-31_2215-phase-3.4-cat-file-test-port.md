## Phase 3.4 - cat-file test port

- Added `tests/t1006-cat-file.sh` as a harness-compatible subset ported from `git/t/t1006-cat-file.sh`.
- Covered v1 `cat-file` capabilities in the port: `-e`, `-t`, `-s`, default/raw output, `-p` pretty output, `--batch`, `--batch-check`, and `--batch-command` (`info`, `contents`, `flush` behavior).
- Added `tests/t1006-cat-file.sh` to `tests/harness/selected-tests.txt` so harness runs include this script.
- Marked `plan.md` item **3.4** complete.

Deferred for v1:

- Object filtering suite (`--filter=...`, `--batch-all-objects`, related atoms) from upstream `t1006`.
- Mailmap-related `cat-file` behavior/tests (`--use-mailmap` paths and mailmap-sensitive output checks).
