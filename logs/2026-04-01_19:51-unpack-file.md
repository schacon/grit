# 2026-04-01 19:51 — grit unpack-file

## Task
Implement `grit unpack-file`: reads a blob OID argument, writes blob content to
a temp file named `.merge_file_XXXXXX` in the current directory, and prints the
path.

## Reference
- `git/builtin/unpack-file.c`: reads OID, writes blob to `.merge_file_XXXXXX`
  temp file via `xmkstemp`, prints path.

## Implementation

- **`grit/src/commands/unpack_file.rs`** — new command module:
  - Resolves OID via `grit_lib::rev_parse::resolve_revision`
  - Reads blob via `repo.odb.read()`
  - Validates type is `ObjectKind::Blob`
  - Creates temp file with `tempfile::Builder::new().prefix(".merge_file_")`
  - Writes blob data, persists with `.keep()`, prints path
- **`grit/src/commands/mod.rs`** — added `pub mod unpack_file;`
- **`grit/src/main.rs`** — added `UnpackFile` variant and match arm
- **`grit/Cargo.toml`** — added `tempfile.workspace = true`
- **`tests/t1051-unpack-file.sh`** — 4 tests: content written, name prefix,
  unknown OID fails
- **`tests/harness/selected-tests.txt`** — added `t1051-unpack-file.sh`

## Results
- `cargo build -p grit`: clean
- `cargo fmt`: clean
- `cargo clippy -p grit`: no new warnings
- `cargo test --workspace`: 5 passed, 0 failed
- `t1051-unpack-file.sh`: 4 passed, 0 failed
