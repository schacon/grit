# Phase 10.5 — update-ref test port and error suite

## Scope

- Completed only `plan.md` item 10.5.
- Ported harness-compatible subsets of:
  - `git/t/t1400-update-ref.sh`
  - `git/t/t1404-update-ref-errors.sh`

## What changed

- Added `tests/t1400-update-ref.sh` with a focused subset covering:
  - ref create/update/delete flows
  - `HEAD` deref behavior
  - `--stdin` create/update and transaction status output
- Added `tests/t1404-update-ref-errors.sh` with a focused subset covering:
  - D/F refname conflicts
  - old-value mismatch failures for update/create/delete batch commands
- Added both scripts to `tests/harness/selected-tests.txt`.
- Fixed `grit update-ref -d` argument handling so old-value verification uses the
  positional old-value for delete mode and rejects extra arguments.
- Marked `plan.md` item 10.5 as complete.

## Validation

- `cargo fmt`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `GUST_BIN=/Users/schacon/projects/grit/target/debug/grit ./tests/harness/run.sh`
