## Phase 8.3 — checkout-index `--temp` / `--tmpdir`

- Implemented real `--temp` behavior in `grit checkout-index` to write blob content into temporary files instead of the working tree.
- Added `--tmpdir <path>` support to direct temporary file placement for scripted callers.
- Made `--stage=all` imply temp-mode output and emit grouped records as:
  `stage1temp stage2temp stage3temp<TAB>path`, using `.` for missing stages.
- Preserved single-stage temp output format as:
  `tempname<TAB>path`.
- Ensured `-u/--index` stat refresh is skipped during temp-mode checkout.
- Marked `plan.md` item `8.3` as complete.

## Verification

- `cargo fmt`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
