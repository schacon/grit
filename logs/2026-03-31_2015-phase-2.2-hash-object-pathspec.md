## Phase 2.2 - hash-object pathspec / --path

- Confirmed and retained upstream-compatible argument behavior for `--path` interactions already wired in `gust hash-object`:
  - reject `--stdin-paths` with `--path`
  - reject `--path` with `--no-filters`
  - allow `--path` with `--stdin`
  - allow `--path` with positional file arguments
- Added focused unit coverage in `gust/src/commands/hash_object.rs` for `--path` cases tied to this phase.
- Updated `plan.md` to mark only item `2.2` as complete.
