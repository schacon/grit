# t0012-help

## Goal

Make `tests/t0012-help.sh` fully pass (121 tests).

## Changes

- Added `commands/upstream_synopsis_help.rs`: central upstream synopsis printing; `rev-parse -h` uses Git’s three-line brief synopsis + parseopt trailer on stdout (harness expects empty stderr); `submodule -h` / `--help` exit 0 (t7400).
- Wired `archive`, `remote`, `range-diff`, `rev-parse` through `try_print_upstream_help_and_exit` before manual parsing.
- `git --list-cmds=builtins`: omit `submodule` and `mergetool` from the builtins union so the loop matches Git’s builtin set (Git lists `submodule` only under list-mainporcelain; `mergetool` is not a C builtin).
- Refactored `main.rs` / `submodule.rs` to use the shared helper instead of duplicate synopsis modules.

## Validation

- `./scripts/run-tests.sh t0012-help.sh` — 121/121
- `cargo test -p grit-lib --lib`
- Spot-check: `t7400-submodule-basic.sh` submodule `-h` test passes
