# t3012-ls-files-dedup

- Read `AGENTS.md`, `PLAN.md`, `git/t/t3012-ls-files-dedup.sh`, and `git/Documentation/git-ls-files.adoc`.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t3012 bash scripts/run-upstream-tests.sh t3012 2>&1 | tail -40`; the first invocation only surfaced stale `/tmp/grit-upstream-workdir` cleanup noise.
- Re-ran the full upstream command `CARGO_TARGET_DIR=/tmp/grit-build-t3012 bash scripts/run-upstream-tests.sh t3012`; result was 3/3 passing.
- Confirmed the upstream artifact at `/tmp/grit-upstream-results/t3012-ls-files-dedup.out` reports all three tests passing.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t3012 cargo fmt --all 2>/dev/null; true`.
- Updated `PLAN.md`, `progress.md`, and `test-results.md` to reflect that `t3012-ls-files-dedup` is complete.
- No Rust source changes were required for this task; the tracked failure count in `PLAN.md` was stale.
