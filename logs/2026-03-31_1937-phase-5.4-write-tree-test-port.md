# Phase 5.4 - write-tree test port

## Goal

Port and pass relevant `write-tree` coverage from upstream `git/t/t0000-basic.sh` and dedicated subdirectory behavior from `git/t/t1020-subdirectory.sh`.

## What changed

- Added `tests/t5000-write-tree.sh` with harness-compatible ports for:
  - basic `write-tree` after `update-index --add`
  - empty-tree write after removal
  - `write-tree --prefix` subtree OID validation
  - missing object failure vs `--missing-ok`
  - running `write-tree` from a subdirectory matching top-level result
- Updated `tests/harness/selected-tests.txt` to include `t5000-write-tree.sh` for this phase harness run.
- Marked `plan.md` item **5.4** as complete (`[x]`).
- Fixed `gust write-tree` behavior needed by the new ports:
  - enforce missing-object validation unless `--missing-ok`
  - fix cache-tree extension encoding to only create directory nodes (skip filename leaf components)
- Resolved CLI argument-ID collision in `gust/src/main.rs` by renaming global `-C` arg ID to `workdir` so `init <directory>` is parsed correctly.

## Verification

Executed successfully:

- `cargo fmt`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo build -p gust`
- `sh tests/harness/run.sh`
