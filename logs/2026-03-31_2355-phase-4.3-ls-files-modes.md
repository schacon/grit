## Phase 4.3 - ls-files remaining modes

- Implemented remaining `gust ls-files` modes in `gust/src/commands/ls_files.rs`:
  - Status modes: `-o`, `-i`, `-m`, `-d`, `-k`, `-u`.
  - Output and filtering: `-z`, `--exclude`, `--exclude-from`, pathspecs.
  - Behavior flags: `--format`, `--deduplicate`, `--error-unmatch`, `--with-tree`.
- Added tree-ish resolution and tree overlay support used by `--with-tree`.
- Expanded `tests/t3004-ls-files-basic.sh` to exercise all required flags from plan item 4.3.
- Added `t3004-ls-files-basic.sh` to `tests/harness/selected-tests.txt` so harness validates this phase.
