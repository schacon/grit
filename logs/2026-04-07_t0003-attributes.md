# t0003-attributes — full pass

## Summary

Made `tests/t0003-attributes.sh` pass 55/55 by aligning gitattributes handling and `check-attr` with Git.

## Key changes

- **grit-lib `attributes`**: Rules carry `attr_base` (directory of the defining `.gitattributes`); matching uses Git-style `match_pathname` / `match_basename` via `wildmatch` with `WM_PATHNAME`. Nested rules only apply under their directory tree (`prepare_attr_stack` semantics). Line length warning uses `>= 2048`. `attr.tree` from config alone ignores bad resolution and falls back to worktree/index. Global `core.attributesFile` follows symlinks. Blob parse replaces literal `\n` sequences when reading tree blobs (compat with grit-stored `test_commit --printf` content). Trim trailing whitespace on `attr=value` tokens.
- **`path_relative_to_worktree`**: Lexical `..` resolution when `canonicalize` fails so relative paths work for missing files.
- **`check-attr`**: Replaced clap positional parsing with manual argv parsing (Git `check-attr.c`) for `--`, `--stdin`, and `--all`.
- **`write_fresh_git_directory`**: Bare repos no longer pre-create top-level `info/` and `hooks/` so `git clone --bare` matches Git (tests use `mkdir info`).

## Validation

- `./scripts/run-tests.sh t0003-attributes.sh` → 55/55
- `cargo fmt`, `cargo check`, `cargo clippy --fix --allow-dirty`, `cargo test -p grit-lib --lib`
