## Task: t4018-diff-funcname

### Claim
- Claimed after completing `t4003-diff-rename-1`.
- Marked as `[~]` in `PLAN.md`.

### Baseline
- `./scripts/run-tests.sh t4018-diff-funcname.sh` reports `1/9` passing (`8` left).
- Direct repro:
  - `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash tests/t4018-diff-funcname.sh`
  - failing tests are:
    - `setup: test-tool userdiff`
    - `last regexp must not be negated`
    - `setup hunk header tests`
    - `hunk header: .gitattributes`
    - `hunk header: A.java`
    - `hunk header: B.java`
    - `hunk header: builtin-drivers`
    - `hunk header: msg`

### Findings so far
- Primary blocker appears to be missing `test-tool userdiff` support in this environment:
  - `error: test-tool: unknown subcommand 'userdiff'`
- Follow-on hunk-header expectations also fail; these likely depend on userdiff driver parsing and custom funcname extraction behavior.

### Status update
- This target is currently blocked by missing test harness functionality (`test-tool userdiff`), not by a single narrow diff-command regression.
- Per queue policy, moved back to pending while continuing with next non-blocked Diff target.

### Additional status after neighboring task work
- `t4133-apply-filenames` was completed in parallel track and marked `[x]`; `t4018` remains the current claimed in-progress target.
- `t4018` still requires deeper work in userdiff/test-tool support and hunk-header extraction behavior; no implementation for these has landed yet.

### Completion validation (2026-04-06)
- Rebuilt current tree:
  - `cargo build --release` ✅
- Local targeted run from `tests/`:
  - `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash t4018-diff-funcname.sh`
  - result: **287/287 passing**
- Tracked harness refresh:
  - `./scripts/run-tests.sh t4018-diff-funcname.sh`
  - result: **287/287 passing**, `data/file-results.tsv` updated
- Upstream isolated harness:
  - `bash scripts/run-upstream-tests.sh t4018-diff-funcname`
  - result: **287/287 passing**
- Quality gates:
  - `cargo fmt` ✅
  - `cargo clippy --fix --allow-dirty` ✅ (reverted unrelated autofixes outside scope)
  - `cargo test -p grit-lib --lib` ✅

### Final status
- `t4018-diff-funcname` is now complete and can be marked `[x]` in `PLAN.md`.
