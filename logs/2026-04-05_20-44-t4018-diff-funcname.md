## Task: t4018-diff-funcname

### Claim
- Claimed after completing `t4003-diff-rename-1`.
- Marked as `[~]` in `PLAN.md`.

### Baseline
- `./scripts/run-tests.sh t4018-diff-funcname.sh` reports `1/9` passing (`8` left).
- Direct repro:
  - `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash tests/t4018-diff-funcname.sh`
  - failing tests are:
    - `setup: test-tool userdiff`
    - `last regexp must not be negated`
    - `setup hunk header tests`
    - `hunk header: .gitattributes`
    - `hunk header: A.java`
    - `hunk header: B.java`
    - `hunk header: builtin-drivers`
    - `hunk header: msg`

### Findings so far
- Primary blocker appears to be missing `test-tool userdiff` support in this environment:
  - `error: test-tool: unknown subcommand 'userdiff'`
- Follow-on hunk-header expectations also fail; these likely depend on userdiff driver parsing and custom funcname extraction behavior.
