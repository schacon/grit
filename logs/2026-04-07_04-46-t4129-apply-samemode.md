## t4129-apply-samemode

### Claim / baseline
- Marked `t4129-apply-samemode` as in-progress (`[~]`) in `PLAN.md`.
- Local baseline: `./scripts/run-tests.sh t4129-apply-samemode.sh` → **7/23**.
- Upstream baseline: `bash scripts/run-upstream-tests.sh t4129-apply-samemode` → **7/23**.

### Progress
- Implemented and validated:
  - `diff --stat -p` now emits patch output in addition to stat lines for both repo and `--no-index` code paths.
  - `apply` patch parser now validates mode headers (`old/new/new file/deleted/index`) with explicit `invalid mode` errors.
  - `apply` parser now captures mode from `index ... <mode>` headers to preserve mode when no explicit `new mode` header exists.
  - mode canonicalization helper added for apply patch mode parsing and index mode writing.
- Current targeted status after latest reruns:
  - `bash scripts/run-upstream-tests.sh t4129-apply-samemode` → **16/23**
  - `./scripts/run-tests.sh t4129-apply-samemode.sh` → **16/23**

### Remaining failures (currently unresolved in this session)
- Upstream remaining failing tests: **10, 11, 13, 14, 16, 21, 23**.
- Current root causes observed from verbose traces:
  - Worktree mode handling still differs from git in permission-sensitive cases (`core.sharedRepository` / `umask` expectations).
  - `format-patch` mode metadata in one path (`index ... 100755`) still diverges under `core.fileMode=false`.
  - `apply --index` missing expected warning text for type mismatch in mode-warning tests.
  - mode restoration/canonicalization for selected mode-only/new-file cases still incomplete.

### Initial notes
- Significant failure set remains around mode-bit handling in `apply` (likely mode-only patches, same-mode transitions, and index/worktree interactions).
- Next step: inspect upstream `.out` failure details and align `grit apply` mode parsing/apply semantics with Git for this file.
