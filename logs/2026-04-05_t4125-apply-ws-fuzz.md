# t4125-apply-ws-fuzz

- Read `AGENTS.md`, the `plan.md` entry, `git/t/t4125-apply-ws-fuzz.sh`, and the `git-apply` whitespace docs.
- Reproduced the failure with `rm -rf /tmp/grit-upstream-workdir /tmp/grit-upstream-results && CARGO_TARGET_DIR=/tmp/grit-build-t4125 bash scripts/run-upstream-tests.sh t4125-apply-ws-fuzz 2>&1 | tail -40`, which started at 2/4 passing.
- Found that `grit apply` accepted `--whitespace=fix` on the CLI but did not use it while locating hunks or comparing old-side context/remove lines.
- Added `WhitespaceMode` handling to `grit/src/commands/apply.rs`, normalized old-side comparisons for `--whitespace=fix`, and rewrote applied context/add lines to their fixed postimage while leaving lines outside the hunk untouched.
- Added focused unit tests for forward application onto already-fixed context and backward application where the out-of-hunk trailing line remains unfixed.
- Verified with `cargo test -p grit-rs --bin grit whitespace_fix -- --nocapture`, `cargo build --release`, and a final upstream rerun showing `t4125-apply-ws-fuzz` at 4/4 passing.
- Attempted to stage the task for `git commit`, but sandbox restrictions blocked Git index writes with `fatal: Unable to create '.git/index.lock': Operation not permitted`, so commit/push could not be completed from this environment.
