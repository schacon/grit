## Task: t4022-diff-rewrite

### Claim
- Claimed from Diff queue after finishing `t4114-apply-typechange`.
- Baseline captured with:
  - `./scripts/run-tests.sh t4022-diff-rewrite.sh`
  - result: **7/11 passing** (4 failing)

### Baseline failures
- `FAIL 2 - detect rewrite` (`git diff-files -B --summary test` rejected `-B`)
- `FAIL 4 - suppress deletion diff with -D` (`git diff -D` treated `-D` as a revision)
- `FAIL 6 - suppress deletion diff with -B -D` (same `-D` parsing gap and no irreversible delete behavior)
- `FAIL 7 - prepare a file that ends with an incomplete line` (`git commit seq -m seq` parsed `-m` as pathspec due trailing var-arg behavior)

### Implementation summary
- `grit/src/commands/diff_files.rs`
  - Added `-B/--break-rewrites` and `--summary` option parsing.
  - Added summary emission for modified rewrites (`rewrite <path> (<N>%)`) and deletions.
  - Wired summary output so `--summary` suppresses default raw output and prints rewrite summary lines.
- `grit/src/commands/diff.rs`
  - Added `-D/--irreversible-delete` flag and trailing-arg recovery.
  - Wired `-D` through patch generation:
    - deleted entries now omit preimage patch body,
    - combined `-B -D` rewrites omit old-side preimage lines in unified output.
  - Added trailing recovery for `-B/--break-rewrites` in re-applied option parsing.
- `grit/src/commands/commit.rs`
  - Removed `trailing_var_arg` from commit pathspec parsing so options after explicit paths (e.g. `commit seq -m seq`) are parsed correctly.

### Validation
- Targeted local test:
  - `TRASH_DIRECTORY=/tmp/t4022-debug-single ... bash t4022-diff-rewrite.sh` → **11/11**
- Tracked harness:
  - `./scripts/run-tests.sh t4022-diff-rewrite.sh` → **11/11**
- Upstream harness:
  - `bash scripts/run-upstream-tests.sh t4022-diff-rewrite` → **11/11**
- Quality gates:
  - `cargo fmt`
  - `cargo clippy --fix --allow-dirty` (reverted unrelated autofixes outside scope)
  - `cargo test -p grit-lib --lib` (96/96)

