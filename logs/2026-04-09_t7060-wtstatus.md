# t7060-wtstatus

- Added `tests/README` (copy of `git/t/README`) so `TEST_DIRECTORY/README` exists for the rename/unmerged setup.
- Status: merge-in-progress banner like Git; unmerged paths section with padded labels; filter staged/unstaged vs unmerged; `-uno` message only when committable; no staged unstage hints during merge; detached `## HEAD (no branch)` in porcelain.
- Untracked: report `dir/` when directory only contains `.git` (nested repo), so `mdconflict/` appears.
- `commit --dry-run`: merge + unmerged output aligned with Git (including footer spacing).
- `diff-index --cached`: emit `U` for unmerged paths; post-process renames from unmerged sources (`-M` → add, `-C` → copy).
- Harness: `./scripts/run-tests.sh t7060-wtstatus.sh` → 17/17 pass.
