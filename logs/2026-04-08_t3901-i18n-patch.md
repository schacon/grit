# t3901-i18n-patch

- **Result:** All 20 tests pass (`./scripts/run-tests.sh t3901-i18n-patch.sh`).
- **Changes (summary):**
  - `ident`: read `GIT_*_NAME` via OS bytes + Latin-1 decode when not UTF-8 (sourced `8859-1.txt`).
  - `format-patch`: `i18n.logOutputEncoding`, `--encoding`, correct `A..B` commit set, RFC2047 decode for stored authors, decode `raw_message` for output, `ISO8859-1` label for charset.
  - `reset`: if first arg fails rev-parse but `refs/heads/<name>` exists, use it (shadowed by worktree file `main`).
  - `cherry_pick` / `revert` / `am` / `commit_tree`: honor `i18n.commitencoding` via `finalize_stored_commit_message`; am `--no-utf8` + `Content-Charset` in state; UTF-8 warning for `am --no-utf8` + ISO patches.
  - `git_commit_encoding`: `finalize_stored_commit_message`, RFC2047 Q decode, `commit_message_unicode_for_display`.
