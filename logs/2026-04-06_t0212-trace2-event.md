## t0212-trace2-event — 2026-04-06

### Goal
Make `tests/t0212-trace2-event.sh` fully pass.

### Baseline
- `./scripts/run-tests.sh t0212-trace2-event.sh` → **0/11**
- Failures indicated missing `test-tool trace2` subcommands and incomplete trace2 event-stream behavior.

### Implementation notes
- Extended `grit/src/main.rs` trace2 support with a process-local event writer:
  - dynamic SID generation with parent SID chaining,
  - command hierarchy tracking,
  - trace2 event target resolution from `GIT_TRACE2_EVENT` and `trace2.eventTarget`,
  - JSON event emission for `version`, `start`, `cmd_name`, `cmd_ancestry`, `def_param`, `data`, `error`, `child_start`, `child_exit`, `exec`, `exit`.
- Implemented missing `test-tool trace2` verbs used by upstream tests:
  - `003error`, `006data`,
  - `300redact_start`, `301redact_child_start`, `302redact_exec`, `303redact_def_param`.
- Added credential redaction (`user:pass@` authority patterns) for URL-bearing fields.
- Implemented `GIT_TRACE2_CONFIG_PARAMS` and `GIT_TRACE2_ENV_VARS` emission as `def_param` events.
- Implemented trace-target directory discard behavior for `GIT_TRACE2_MAX_FILES`:
  - writes `git-trace2-discard` with `version` then `too_many_files`.
- Updated `004child` handling:
  - emits `child_start`/`child_exit`,
  - propagates parent SID/hierarchy env to child,
  - emits explicit `exit` before process exit for commands that terminate via `exit_with_status`.
- Normalized `start.argv` to match upstream test expectations:
  - strips the outer `test-tool` token for `test-tool trace2 ...` invocations.

### Validation
- `cargo build --release -p grit-rs` ✅
- `GUST_BIN=/workspace/target/release/grit TEST_VERBOSE=1 bash t0212-trace2-event.sh` ✅ **11/11**
- `./scripts/run-tests.sh t0212-trace2-event.sh` ✅ **11/11**

### Targeted regressions
- `./scripts/run-tests.sh t0213-trace2-ancestry.sh` ✅ **5/5**
- `./scripts/run-tests.sh t1411-reflog-show.sh` ✅ **17/17**
- `./scripts/run-tests.sh t0211-trace2-perf.sh` ✅ **4/17** (unchanged baseline)
- `./scripts/run-tests.sh t0210-trace2-normal.sh` ✅ **0/14** (unchanged baseline)

### Result
- `t0212-trace2-event` now fully passing.
