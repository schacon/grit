# t3202-show-branch

- Wired `reset` reflog identity through `resolve_reflog_identity` so `GIT_COMMITTER_DATE` matches commits (avoids mixed wall-clock vs test_tick timestamps).
- `show-branch --reflog`: anchor relative times from work tree `.test_tick` when present; use `show_date_relative` with that anchor; default graph `--more` to 0 in reflog mode (no stray branch tips in graph).
- `reflog expire` / `gc` reflog expiry: use wall-clock `now` for cutoffs so `--expire=now` prunes harness entries; `GIT_TEST_DATE_NOW` is for display only.
- Harness: `./scripts/run-tests.sh t3202-show-branch.sh` → 27/27; CSV + dashboards refreshed.
