# t2050-git-dir-relative — Log

**Date:** 2026-04-05
**Branch:** main
**Status:** PASS (4/4 tests)

## Summary

Re-verified `t2050-git-dir-relative` on `main` and found the underlying fix was
already present in the current tree. The remaining work was stale bookkeeping:
the upstream result was 4/4 passing, but `PLAN.md` had not been marked done.

## Verification

1. Read `AGENTS.md`, `PLAN.md`, and `git/t/t2050-git-dir-relative.sh`
2. Ran `rm -rf /tmp/grit-upstream-workdir /tmp/grit-upstream-results && CARGO_TARGET_DIR=/tmp/grit-build-t2050-git-dir-relative bash scripts/run-upstream-tests.sh t2050-git-dir-relative 2>&1 | tail -40`
3. Confirmed `Tests: 4 (pass: 4, fail: 0)`
4. Ran `CARGO_TARGET_DIR=/tmp/grit-build-t2050-git-dir-relative cargo fmt --all 2>/dev/null; true`
5. Updated `PLAN.md`, `progress.md`, and `test-results.md`

## Notes

- No Rust source changes were required for this task.
- The worktree already contained unrelated edits; this task only updated the
  stale tracking files and added this log entry.
