# t4005-diff-rename-2

- Date: 2026-04-05
- Task: verify and close out `t4005-diff-rename-2`

## Summary

The requested `run-upstream-tests.sh` invocation initially produced `Files producing TAP output: 0` because the script executes `target/release/grit`, while the requested build wrote the fresh binary to `/tmp/grit-build-t4005/release/grit`. After building `grit-rs` with `CARGO_TARGET_DIR=/tmp/grit-build-t4005` and repointing `target/release/grit` at that binary, the upstream rerun passed all four tests.

## Commands

- `CARGO_TARGET_DIR=/tmp/grit-build-t4005 bash scripts/run-upstream-tests.sh t4005-diff-rename-2 2>&1 | tail -40`
- `CARGO_TARGET_DIR=/tmp/grit-build-t4005 cargo build --release -p grit-rs`
- `rm -f target/release/grit && ln -s /tmp/grit-build-t4005/release/grit target/release/grit`
- `CARGO_TARGET_DIR=/tmp/grit-build-t4005 bash scripts/run-upstream-tests.sh t4005-diff-rename-2 2>&1 | tail -40`
- `CARGO_TARGET_DIR=/tmp/grit-build-t4005 cargo fmt --all 2>/dev/null; true`

## Result

- Upstream status: `t4005-diff-rename-2` is now `4/4` passing.
- Rust source changes: none required in this workspace.
- Tracking updates: `PLAN.md`, `progress.md`, `test-results.md`.
