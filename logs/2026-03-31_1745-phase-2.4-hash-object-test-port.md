# Phase 2.4 - t1007 hash-object port

## Scope

- Complete `plan.md` item `2.4` only by porting `git/t/t1007-hash-object.sh` into `tests/` and getting it green in the local harness.

## Changes made

- Added `tests/t1007-hash-object.sh` as a harness-compatible upstream port subset:
  - switched command invocations from `git` to `grit`
  - preserved coverage for argument validation, file/stdin/stdin-paths hashing, `-w`, and invalid object/type handling
- Updated `tests/harness/selected-tests.txt` to run `t1007-hash-object.sh`.
- Fixed `tests/test-lib.sh` `test_must_fail` so expected-failure checks work under `set -e`.
- Marked `plan.md` item `2.4` as done (`[x]`).

## Verification

- `./tests/harness/run.sh` (includes `t1007-hash-object.sh`) passes.
- `cargo fmt` passes.
- `cargo clippy --workspace --all-targets -- -D warnings` passes.
- `cargo test --workspace` passes.
