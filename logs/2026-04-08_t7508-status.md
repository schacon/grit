# t7508-status progress

## Changes

- Added `grit/src/git_column.rs`: Git `column.c`-style layout for untracked/ignored long status; wired `--column` / `--no-column`, `column.status` / `column.ui`, `finalize_colopts`.
- `status`: `status.relativePaths`, porcelain without spurious `##`, short format only shows `!!` with `--ignored`, `-z` uses unquoted paths, `core.quotePath` for short output, correct cwd-relative paths via `diff_paths_relative`, skip `.test_tick`/`.test_oid_cache` in untracked walk, `-vv` matches Git (mnemonic diff + headers).
- `diff`: `diff.mnemonicprefix` uses `c/`/`i/` for `--cached`.
- `commit --dry-run`: in-memory pathspec staging (no index write), branch/upstream header via shared helper, pathspec-filtered staged/unstaged/untracked sections, orphan shows `Initial commit`.
- `DiffEntry`: `PartialEq`/`Eq` for dry-run splitting.

## Test

`./scripts/run-tests.sh t7508-status.sh` → **85/126** passing (was 39/126 at start of session per CSV).

Remaining failures are mostly submodule summary, stash, optional locks, racy timestamps, fsmonitor/untrackedCache advice.
