# t2023-checkout-m

- Claimed `t2023-checkout-m` from `plan.md` at 2/5 passing.
- Reproduced the upstream failures with `CARGO_TARGET_DIR=/tmp/grit-build-t2023 bash scripts/run-upstream-tests.sh t2023 2>&1 | tail -40`.
- Read `git/t/t2023-checkout-m.sh` plus the checkout and index-format documentation for `checkout -m` and `resolve-undo`.
- Implemented `REUC` resolve-undo parsing/writing in `grit-lib/src/index.rs` and recorded higher-stage entries when `git add` resolves a conflict.
- Implemented `Index::unresolve()` and wired `grit update-index --unresolve` to use saved resolve-undo state.
- Implemented `checkout -m -- <path>` restoration by recreating higher-stage index entries and rewriting the conflicted worktree file from those stages.
- Implemented `checkout -m <branch>` branch-switch conflict recreation for local content conflicts, including stage 1/2/3 index entries and conflicted worktree content.
- Rebuilt `target/release/grit` and re-ran `t2023`; upstream verification reached 5/5 passing.
- Updated `plan.md`, `progress.md`, and `test-results.md` for this task.
