# t1501-work-tree

## Summary

Made `tests/t1501-work-tree.sh` fully pass (39/39).

## Changes (high level)

- **Repository discovery**: validate bogus absolute `GIT_WORK_TREE`; preserve work tree for gitfile + `commondir` discovery when `core.worktree` would be wrong (multi-worktree tests).
- **rev-parse**: `--git-common-dir` prints canonical absolute path; `PathError` from lib maps to `fatal:` + exit 128 in the binary.
- **test-tool**: implemented `subprocess --setup-work-tree` (chdir to work tree, pin `GIT_DIR`/`GIT_WORK_TREE`, re-exec grit).
- **diff**: pathspecs resolved with cwd prefix; zero-OID tree side reads worktree; `--cached` empty new file omits hunk body; `diff-index` worktree merge only when tree matches index; raw uncached Added lines use all-zero OIDs for harness.
- **ls-files**: empty literal pathspec from `:/` matches all paths.
- **grep**: output paths strip `show_prefix` like Git when cwd is under the work tree.

## Validation

- `./scripts/run-tests.sh t1501-work-tree.sh` — 39 pass
- `cargo test -p grit-lib --lib` — pass
