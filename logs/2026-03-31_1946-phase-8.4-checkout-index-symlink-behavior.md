## Phase 8.4 - checkout-index symlink/no-symlink behavior

- Scope: Implement only `plan.md` item `8.4`.
- Updated `grit checkout-index` to honor repository config `core.symlinks`:
  - `core.symlinks=true` (default): checkout index mode `120000` as a real symlink.
  - `core.symlinks=false`: checkout index mode `120000` as a regular file containing the blob payload (the symlink target text), matching upstream `t2005` expectation.
- Added a focused unit test in `grit/src/commands/checkout_index.rs` that sets `core.symlinks=false`, checks out a mode `120000` index entry, and verifies:
  - the working tree path is a regular file (not a symlink),
  - the file content matches the blob payload.

## Verification

- `cargo fmt`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`

All commands completed successfully.
