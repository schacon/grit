## Task: t4003-diff-rename-1

### Claim
- Claimed after completing `t4152-am-subjects`.
- Marked as `[~]` in `PLAN.md`.

### Baseline
- `./scripts/run-tests.sh t4003-diff-rename-1.sh` reports `4/7` passing (`3` left).
- Direct repro:
  - `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash tests/t4003-diff-rename-1.sh`
  - failing tests are:
    - `validate output from rename/copy detection (#1)`
    - `validate output from rename/copy detection (#2)`
    - `validate output from rename/copy detection (#3)`

### Findings so far
- Rename/copy detection itself is working, but the failing expectation is in patch hunk shape:
  - expected output uses `GIT_DIFF_OPTS=--unified=0` one-line hunks,
  - actual output prints wider-context hunks (e.g. `@@ -3,7 +3,7 @@`) in these rename/copy patch sections.
- Next implementation should focus on plumbing `GIT_DIFF_OPTS=--unified=0` into `diff-index -p` output for rename/copy paths.
