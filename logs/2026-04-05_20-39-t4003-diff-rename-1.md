## Task: t4003-diff-rename-1

### Claim
- Claimed after completing `t4152-am-subjects`.
- Marked as `[~]` in `PLAN.md`, then promoted to `[x]` after fix validation.

### Baseline
- `./scripts/run-tests.sh t4003-diff-rename-1.sh` reports `4/7` passing (`3` left).
- Direct repro:
  - `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash tests/t4003-diff-rename-1.sh`
  - failing tests are:
    - `validate output from rename/copy detection (#1)`
    - `validate output from rename/copy detection (#2)`
    - `validate output from rename/copy detection (#3)`

### Findings so far
- Rename/copy detection itself is working, but the failing expectation is in patch hunk shape:
  - expected output uses `GIT_DIFF_OPTS=--unified=0` one-line hunks,
  - actual output prints wider-context hunks (e.g. `@@ -3,7 +3,7 @@`) in these rename/copy patch sections.
- Next implementation should focus on plumbing `GIT_DIFF_OPTS=--unified=0` into `diff-index -p` output for rename/copy paths.

### Fix implemented
- Updated `grit/src/commands/diff_index.rs`:
  - Added `diff_context_from_env()` helper to parse `GIT_DIFF_OPTS` context controls:
    - `--unified=<n>`
    - `-U<n>`
    - `-u<n>`
  - `parse_options` now seeds `context_lines` from `GIT_DIFF_OPTS` (default still `3` when unset).
  - CLI-provided `-U` / `--unified` values continue to override as before.

### Validation
- `cargo build --release` -> pass.
- `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash tests/t4003-diff-rename-1.sh` -> `7/7` pass.
- `./scripts/run-tests.sh t4003-diff-rename-1.sh` -> `7/7` pass; `data/file-results.tsv` refreshed.
- Regression checks:
  - `./scripts/run-tests.sh t4007-rename-3.sh` -> `13/13` pass.
  - `./scripts/run-tests.sh t4006-diff-mode.sh` -> `7/7` pass.
  - `./scripts/run-tests.sh t4044-diff-index-unique-abbrev.sh` -> still `0/2` (known harness blocker; unchanged).
