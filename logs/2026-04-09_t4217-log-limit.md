# t4217-log-limit (2026-04-09)

- Failure: `git log --since-as-filter` filtered out all commits because `GIT_COMMITTER_DATE="YYYY-MM-DD HH:MM"` was left as a raw tail on the committer line when `parse_date_to_git_timestamp` returned `None`.
- Fix: After existing parsers, fall back to `grit_lib::git_date::parse::parse_date` so loose Git dates match upstream and serialize to `<epoch> <±HHMM>`.
- Verified: `./scripts/run-tests.sh t4217-log-limit.sh` (3/3), `cargo test -p grit-lib --lib`.
