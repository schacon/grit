# t3423-rebase-reword

- Claimed `t3423-rebase-reword` from `plan.md` at 1/3 and inspected `git/t/t3423-rebase-reword.sh`.
- The requested `CARGO_TARGET_DIR=/tmp/grit-build-t3423-rebase-reword bash scripts/run-upstream-tests.sh t3423-rebase-reword 2>&1 | tail -40` command did not emit TAP in this sandbox, so I used a direct isolated run of `git/t/t3423-rebase-reword.sh` to get a real pass/fail signal.
- Interactive rebase already had most of the needed scaffolding in `grit/src/commands/rebase.rs`; the missing behavior was allowing dropped commits in the edited todo list and invoking sequence/commit editors correctly when the scratch path contains spaces.
- The remaining test failure was in `git checkout --theirs file-2`: path checkout worked with `--`, but the single-positional form used by the upstream test still flowed through branch/commit parsing. `grit/src/commands/checkout.rs` now forces `--ours` / `--theirs` / `--merge` with one positional argument into path mode.
- Rebuilt `target/release/grit`, reran the direct isolated upstream test, and confirmed `t3423-rebase-reword.sh` passes `3/3`.
- Ran the requested formatting command: `CARGO_TARGET_DIR=/tmp/grit-build-t3423-rebase-reword cargo fmt --all 2>/dev/null; true`.
