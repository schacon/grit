# t3423-rebase-reword

- Claimed `t3423-rebase-reword` from `plan.md` at 1/3 and inspected `git/t/t3423-rebase-reword.sh`.
- Reproduced the requested upstream failure with `CARGO_TARGET_DIR=/tmp/grit-build-t3423 bash scripts/run-upstream-tests.sh t3423 2>&1 | tail -40` (`1/3` initially, then `2/3` after the first rebase patch).
- Implemented scoped interactive rebase support in `grit/src/commands/rebase.rs` for `pick` and `reword`, including sequence-editor todo parsing, commit-message editing for `reword`, and preserving the `reword` action through conflict resolution so `git rebase --continue` reopens the editor.
- Traced the remaining failure to `git checkout --theirs file-2` during conflict resolution and wired `--ours` / `--theirs` path checkout in `grit/src/commands/checkout.rs`, including the single-positional fallback form used by the test.
- Rebuilt `target/release/grit` and confirmed `CARGO_TARGET_DIR=/tmp/grit-build-t3423 bash scripts/run-upstream-tests.sh t3423 2>&1 | tail -40` now reports `3/3` passing.
- Ran the requested formatting command: `CARGO_TARGET_DIR=/tmp/grit-build-t3423 cargo fmt --all 2>/dev/null; true`.
