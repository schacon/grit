# t4006-diff-mode

- Claimed `t4006-diff-mode` from `PLAN.md` and read `AGENTS.md`, the plan entry, `git/t/t4006-diff-mode.sh`, and `git-diff` documentation.
- Reproduced the upstream failure with `CARGO_TARGET_DIR=/tmp/grit-build-t4006 bash scripts/run-upstream-tests.sh t4006-diff-mode 2>&1 | tail -40`; result was 6/7 passing with the remaining failure in `git diff HEAD --stat` for mixed binary and text mode-only changes.
- Inspected `grit/src/commands/diff.rs` and confirmed `write_stat` was treating binary mode-only changes as text with a `0` count instead of `Bin`.
- Updated `write_stat` to classify binary entries from raw content, emit `Bin` rows, and widen the count column to three characters when any binary entry is present so mixed `Bin`/`0` output matches upstream spacing.
- Rebuilt with `CARGO_TARGET_DIR=/tmp/grit-build-t4006 cargo build --release -p grit-rs`, refreshed `target/release/grit`, and reran the upstream file.
- Verified `CARGO_TARGET_DIR=/tmp/grit-build-t4006 bash scripts/run-upstream-tests.sh t4006-diff-mode 2>&1 | tail -40` now reports 7/7 passing.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t4006 cargo fmt --all 2>/dev/null; true`.
