<<<<<<< HEAD
# t1600-index

- Claimed and verified the `t1600-index` task from `plan.md`.
- Reproduced the initial upstream state at 5/7 passing, with failures in `index.skipHash config option` and `index version config precedence`.
- Updated `grit-lib/src/index.rs` to:
  - default invalid index-version fallbacks to version 2,
  - accept zeroed trailing index hashes,
  - honor `index.skipHash` and `feature.manyFiles` when writing indexes,
  - write on-disk index version 4 with pathname compression.
- Added `test-tool hexdump` support in `grit/src/main.rs` for upstream helper usage.
- Fixed the upstream runner's generated `t/helper/test-tool` wrapper in `scripts/run-upstream-tests.sh` to embed the active `grit` binary path.
- Rebuilt `target/release/grit` and re-ran `CARGO_TARGET_DIR=/tmp/grit-build-t1600 bash scripts/run-upstream-tests.sh t1600`.
- Ran `cargo fmt` and `cargo test -p grit-lib --lib` (97/97 passing).
- `CARGO_TARGET_DIR=/tmp/grit-build-t1600 cargo clippy --fix --allow-dirty` is blocked in this sandbox because Cargo cannot bind its lock-management TCP listener (`Operation not permitted`).
- Final result: `t1600-index` passes 7/7.
=======
## t1600-index — 2026-04-05

- Claimed target from plan as next high-value near-complete file (`4/7`).
- Reproduced failures with:
  - `./scripts/run-tests.sh t1600-index.sh` (5/7)
  - `GUST_BIN=/workspace/tests/grit TEST_VERBOSE=1 bash tests/t1600-index.sh`
- Root causes identified:
  1. Harness helper gap: `test_trailing_hash` function missing from simplified `tests/test-lib.sh` caused case 6 to fail before exercising product behavior.
  2. Index serialization mismatch: v4 writing was downgraded to v2 in `grit-lib/src/index.rs`, causing config precedence checks to observe `2` instead of expected `4`.
  3. `index.skipHash` compatibility missing in index writer and parser: trailing hash was always validated/written as SHA-1, not allowing all-zero trailer behavior expected by upstream test.

- Implemented fixes:
  - `grit-lib/src/index.rs`
    - Added `index_skip_hash()` config lookup (`index.skipHash`, fallback `feature.manyFiles`).
    - `Index::write()` now writes zero trailing hash bytes when skip-hash is enabled.
    - `Index::parse()` now accepts all-zero trailing hash as valid (matches skip-hash semantics).
    - Enabled true index version 4 writing with path compression:
      - Serialize v4 entries using shared-prefix compression + varint strip length.
      - Added `shared_prefix_len()` and `write_varint()` helpers.
      - Kept v2/v3 serialization path unchanged.
    - Updated v4 unit test expectation to assert version 4 on disk.
  - `grit/src/main.rs`
    - Added `test-tool hexdump` implementation for upstream helper compatibility.
    - Wired `hexdump` into `test-tool` subcommand dispatch.
  - `tests/test_trailing_hash`
    - Added executable helper command in `tests/` to satisfy simplified harness behavior where function fallback was absent.

- Validation:
  - `cargo fmt`
  - `cargo build --release -p grit-rs` ✅
  - `cargo clippy --fix --allow-dirty` ✅ (reverted unrelated known churn files afterwards)
  - `cargo test -p grit-lib --lib` ✅ (96/96)
  - `GUST_BIN=/workspace/tests/grit TEST_VERBOSE=1 bash tests/t1600-index.sh` ✅ (7/7)
  - `./scripts/run-tests.sh t1600-index.sh` ✅ (7/7)

- Tracking updates pending with this increment:
  - `PLAN.md` entry for `t1600-index` to `[x]`.
  - `progress.md` counts/list refresh.
  - `test-results.md` append the new executed commands/results.
>>>>>>> pr-7
