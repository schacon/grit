# t1600-index

- Claimed and verified the `t1600-index` task from `plan.md`.
- Reproduced the initial upstream state at 5/7 passing, with failures in `index.skipHash config option` and `index version config precedence`.
- Updated `grit-lib/src/index.rs` to:
  - default invalid index-version fallbacks to version 2,
  - accept zeroed trailing index hashes,
  - honor `index.skipHash` and `feature.manyFiles` when writing indexes,
  - write on-disk index version 4 with pathname compression.
- Added `test-tool hexdump` support in `grit/src/main.rs` for upstream helper usage.
- Fixed the upstream runner's generated `t/helper/test-tool` wrapper in `scripts/run-upstream-tests.sh` to embed the active `grit` binary path.
- Rebuilt `target/release/grit` and re-ran `CARGO_TARGET_DIR=/tmp/grit-build-t1600 bash scripts/run-upstream-tests.sh t1600`.
- Ran `cargo fmt` and `cargo test -p grit-lib --lib` (97/97 passing).
- `CARGO_TARGET_DIR=/tmp/grit-build-t1600 cargo clippy --fix --allow-dirty` is blocked in this sandbox because Cargo cannot bind its lock-management TCP listener (`Operation not permitted`).
- Final result: `t1600-index` passes 7/7.
