# t7005-editor

- Added `grit/src/editor.rs`: `is_terminal_dumb`, `resolve_git_editor` matching Git `editor.c` (skip `VISUAL` on dumb/unset `TERM`, no `vi` fallback on dumb).
- `for_launch`: ignore harness `EDITOR`/`VISUAL` of `:` when launching editors so `PATH`-only `vi` works; `git var GIT_EDITOR` still prints `:` (second arg `false`).
- `commit`: `--amend` with editor now writes `COMMIT_EDITMSG` and runs editor instead of reusing HEAD message without edit; removed stdin-is-tty `vi`→`true` hack (broke harness).
- `rebase`: reuse shared resolver.
- Verified: `./scripts/run-tests.sh t7005-editor.sh` 12/12; `cargo test -p grit-lib --lib`.
