## Phase 8.1 checkout-index --mkdir

- Reviewed `plan.md` item `8.1`, `AGENT.md`, `git/Documentation/git-checkout-index.adoc`, and `git/builtin/checkout-index.c`.
- Confirmed `gust checkout-index` already handled:
  - checking out all entries via `-a/--all`
  - checking out listed entries via argv paths or `--stdin`
- Fixed directory creation behavior in `gust/src/commands/checkout_index.rs`:
  - before: always created leading directories
  - after: creates leading directories only when `--mkdir` is passed
  - without `--mkdir`, command now errors when leading directories are missing
- Updated `plan.md`: marked only `8.1` as complete (`[x]`).

## Validation

- `cargo fmt`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`

All checks passed.
