## t6601-path-walk (in progress)

Date: 2026-04-06

### Claim
- Claimed `t6601-path-walk` as the current Rev Machinery task.

### Baseline
- Harness baseline:
  - `./scripts/run-tests.sh t6601-path-walk.sh` → **1/15**.
- Direct baseline:
  - `GUST_BIN=/workspace/target/release/grit bash tests/t6601-path-walk.sh` → **1/15**.

### Initial diagnosis
- Primary blocker is missing test-tool functionality:
  - `error: test-tool: unknown subcommand 'path-walk'`
- Follow-on assertion failures (`out` missing, etc.) are cascading from that missing subcommand.

### Next implementation focus
- Implement `test-tool path-walk` in `grit` with support for:
  - object-type toggles (`--no-blobs`, `--no-commits`, `--no-tags`, `--no-trees`),
  - path pruning switches (`--prune`, `--edge-aggressive`),
  - sparse pattern input (`--stdin-pl`),
  - revision-option passthrough semantics used by `t6601` (`--all`, `--branches`, `--indexed-objects`, `--boundary`, `--not`, and explicit revisions).
