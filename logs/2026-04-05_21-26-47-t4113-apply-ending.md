# t4113-apply-ending

- Read `AGENTS.md`, `plan.md`, `git/t/t4113-apply-ending.sh`, and `git/Documentation/git-apply.adoc`.
- Reproduced the upstream failure: `CARGO_TARGET_DIR=/tmp/grit-build-t4113 bash scripts/run-upstream-tests.sh t4113-apply-ending` initially reported 2/3 passing with `apply at the beginning` failing.
- Located the bug in `grit/src/commands/apply.rs`: `apply_hunks()` let a line-1 insertion hunk scan forward and apply after the first line, which duplicated the first line instead of rejecting the patch.
- Patched `apply_hunks()` to reject a start-anchored hunk whose first meaningful line is an addition and which only matches after sliding forward.
- Rebuilt `target/release/grit` with `cargo build --release -p grit-rs`.
- Re-ran `CARGO_TARGET_DIR=/tmp/grit-build-t4113 bash scripts/run-upstream-tests.sh t4113-apply-ending 2>&1 | tail -40`; result: 3/3 passing.
- Ran `CARGO_TARGET_DIR=/tmp/grit-build-t4113 cargo fmt --all 2>/dev/null; true`.
- Updated `plan.md`, `progress.md`, and `test-results.md`.
