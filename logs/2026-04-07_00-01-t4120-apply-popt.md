## t4120-apply-popt (claim/baseline)

### Claim

- Claimed next Diff target from plan: `t4120-apply-popt`.
- Updated plan status from `[ ]` to `[~]`.

### Baseline

- `./scripts/run-tests.sh t4120-apply-popt.sh` -> 3/12 passing.
- `bash scripts/run-upstream-tests.sh t4120-apply-popt` -> 5/12 passing.

### Initial failing assertions snapshot (upstream)

- 1: setup failed while preparing escaped-path fixture.
- 3/4/5: `git apply -p` should reject malformed/non-numeric/negative values with git-compatible error.
- 7/9: `git apply --stat -p3` should fail with `removing 3 leading` diagnostics for over-trimmed paths.
- 12: rename-only patch with `-p2` should materialize target path correctly.

### Implementation notes

- Added strict `-p<n>` argument validation in `grit apply`:
  - non-numeric, mixed, and negative values now fail with `option -p expects a non-negative integer`.
- Added early path-strip validation using patch header paths so over-stripping fails with git-compatible diagnostics:
  - `removing <n> leading pathname component(s) from "<path>"`.
- Applied strip validation consistently to escaped/non-ASCII patch headers as well.
- Fixed metadata-only rename/copy apply behavior to materialize target content from source paths (instead of skipping these sections).
- Escaped non-ASCII bytes in diff header/rename/copy path rendering (`\ooo` octal form) so fixtures like `süb` match upstream `grep "[\\]"` expectations.
- Updated `mv` index stat refresh strategy to write smudged stat fields after rename; this forces content verification instead of racy stat trust in immediate follow-up diffs.

### Validation

- `cargo build --release` ✅
- `./scripts/run-tests.sh t4120-apply-popt.sh` → `9/12` (local mirror partial due simplified helper behavior; remaining failures are mode-only follow-up checks that rely on upstream `test_chmod` semantics not present in this mirror).
- `bash scripts/run-upstream-tests.sh t4120-apply-popt` → `12/12` ✅
- Direct isolated run:
  - `cd /tmp/grit-upstream-workdir/t && GIT_BUILD_DIR=/tmp/grit-upstream-workdir TEST_NO_MALLOC_CHECK=1 TAR=tar timeout 60 bash ./t4120-apply-popt.sh` → `12/12` ✅
- Quality gates:
  - `cargo fmt` ✅
  - `cargo clippy --fix --allow-dirty` ✅ (reverted unrelated autofixes)
  - `cargo test -p grit-lib --lib` ✅ (`96/96`)
