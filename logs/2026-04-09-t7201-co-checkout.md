# t7201-co checkout work (2026-04-09)

## Done

- Extended `merge_trees_three_way` with `TreeMergeConflictPresentation` (labels + conflict style); updated cherry-pick/revert callers.
- Checkout: `branch.autoSetupMerge` for `checkout -b` without explicit start (HEAD); `--track` validation (detached HEAD, tag start); `checkout --track origin/koala/bear` DWIM local branch name.
- Ambiguous tag vs branch: `checkout tags/name` resolves to tag.
- Post-branch-switch local change lines: `diff_index_to_worktree` vs new HEAD, printed to stdout before tracking info on stderr (matches Git ordering for t7201 #4).
- Path checkout: tri-state merge flags from raw argv (`--no-merge`, `--conflict`, `--no-conflict`); `--ours`/`--theirs` reparsing; root `.` handling for unmerged paths; `-f` multi-arg pathspec heuristic; `-f` only updates paths with stage 0 for unmerged entries; `--ours`/`--theirs` write worktree without clearing unmerged index stages.
- `create_and_switch_branch`: validate `--track` start before creating branch ref.
- `execute_custom_merge_driver` in merge.rs is `pub(crate)` for checkout (path merge still WIP for full t7201).
- `merge.rs`: expose `execute_custom_merge_driver`.

## Remaining (18 failing in t7201)

- Branch `checkout -m` / merge-ort parity (tests 5–10, 32–37, 41, 45): need Git-equivalent three-way tree merge + worktree/index result.
- Invalid `--conflict` and `--no-merge`/`--no-conflict` combinations (38–40, 42): align option parsing with Git.

Harness after changes: **28/46** passing (`./scripts/run-tests.sh t7201-co.sh`).
