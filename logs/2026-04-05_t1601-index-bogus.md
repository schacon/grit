## t1601-index-bogus

- Read `/Users/schacon/projects/grit/AGENTS.md`, the `t1601-index-bogus` entry in `plan.md`, and upstream `git/t/t1601-index-bogus.sh`.
- Ran the requested command:
  `CARGO_TARGET_DIR=/tmp/grit-build-t1601 bash scripts/run-upstream-tests.sh t1601 2>&1 | tail -40`
  which reported `2/4` passing against `/Users/schacon/projects/grit/target/release/grit`.
- Reproduced the failing upstream assertions directly and confirmed tests 2 and 3 were failing:
  `read-tree refuses to read null sha1` and `GIT_ALLOW_NULL_SHA1 overrides refusal`.
- Inspected `grit/src/commands/read_tree.rs` and found the null-SHA1 guard was already present in the source:
  `tree_to_index_entries()` rejects zero OIDs unless `GIT_ALLOW_NULL_SHA1=1`.
- Built the current source with `CARGO_TARGET_DIR=/tmp/grit-build-t1601 cargo build --release` and verified the freshly built binary behaved correctly:
  default `read-tree` failed with `entry 'broken' has a null sha1`, and `GIT_ALLOW_NULL_SHA1=1` succeeded.
- Rebuilt the repo-default release binary with `cargo build --release`, reran the upstream harness, and confirmed `t1601-index-bogus` passes `4/4`.
- Ran `cargo fmt` successfully.
- Attempted `CARGO_TARGET_DIR=/tmp/grit-build-t1601 cargo clippy --fix --allow-dirty`, but the sandbox blocked Cargo's TCP-based lock manager setup with `Operation not permitted (os error 1)`.
- Updated `plan.md` and `progress.md` to mark `t1601-index-bogus` complete.
- Attempted to stage and commit the result, but the sandbox blocked Git from creating `.git/index.lock`, so `git add`/`git commit` could not run in this environment.
- Attempted `openclaw system event --text "Done: t1601-index-bogus" --mode now`, but it failed with `SecItemCopyMatching failed -50`.
