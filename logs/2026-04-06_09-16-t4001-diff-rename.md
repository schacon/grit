## Task: t4001-diff-rename

### Claim
- Claimed after completing `t4104-apply-boundary`.
- Marked as `[~]` in `PLAN.md`.

### Baseline
- Tracked harness:
  - `./scripts/run-tests.sh t4001-diff-rename.sh`
  - result: **13/23 passing** (10 failing)
- Direct local run from `tests/`:
  - `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash t4001-diff-rename.sh`
  - result: **13/23 passing** (same 10 failing tests)

### Current failing assertions
- 10 `favour same basenames over different ones`
- 11 `test diff.renames=true for git status`
- 12 `test diff.renames=false for git status`
- 13 `favour same basenames even with minor differences`
- 14 `two files with same basename and same content`
- 15 `setup for many rename source candidates`
- 17 `rename pretty print with common prefix`
- 18 `rename pretty print with common suffix`
- 19 `rename pretty print with common prefix and suffix`
- 20 `rename pretty print common prefix and suffix overlap`

### Initial observations
- Earlier rename-diff assertions (including patch output validation) pass, so core diff-index rename detection is partially working.
- Remaining failures cluster in:
  - rename reporting in `status`,
  - rename/copy candidate prioritization and limit behavior (`-l`, basename preference),
  - pretty-print formatting for rename summaries (`{old => new}` style compaction).

### Completion validation (2026-04-06)
- Rebuilt current tree:
  - `cargo build --release` ✅
- Local targeted run from `tests/`:
  - `EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash t4001-diff-rename.sh`
  - result: **23/23 passing**
- Tracked harness refresh:
  - `./scripts/run-tests.sh t4001-diff-rename.sh`
  - result: **23/23 passing**, `data/file-results.tsv` updated
- Upstream isolated harness:
  - `bash scripts/run-upstream-tests.sh t4001-diff-rename`
  - result: **23/23 passing**
- Regression checks:
  - `./scripts/run-tests.sh t4104-apply-boundary.sh` => 24/24
  - `./scripts/run-tests.sh t4018-diff-funcname.sh` => 287/287
  - `./scripts/run-tests.sh t4039-diff-assume-unchanged.sh` => 4/4
  - `./scripts/run-tests.sh t4007-rename-3.sh` => 13/13
- Quality gates:
  - `cargo fmt` ✅
  - `cargo clippy --fix --allow-dirty` ✅ (reverted unrelated autofixes outside scope)
  - `cargo test -p grit-lib --lib` ✅

### Final status
- `t4001-diff-rename` is complete and marked `[x]` in `PLAN.md`.
