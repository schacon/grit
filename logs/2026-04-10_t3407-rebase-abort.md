# t3407-rebase-abort

## Summary

Made `t3407-rebase-abort.sh` pass (17/17).

## Changes

- **prune**: Seed reachability from `rebase-apply|rebase-merge` `orig-head` and `autostash` (matches Git `reachable.c`) so `git prune --expire=now` does not delete `orig-head` before `rebase --abort`.
- **reflog expire**: Parse `--expire` values like `$GIT_COMMITTER_DATE` (`<unix> <tz>`) via `parse_date_basic`.
- **rebase**: `--abort` conflicts with `-v`/`--verbose` (clap); `replay_remaining` advances todo after a successful pick; cherry-pick merge base uses onto-tree when `HEAD` has moved past stored onto; three-way merge no longer takes `theirs` when base≠ours but base==theirs; upstream/onto resolution uses `resolve_revision_as_commit` for annotated tags in `--onto` ranges.

## Validation

- `./scripts/run-tests.sh t3407-rebase-abort.sh` — all pass
- `cargo test -p grit-lib --lib` — pass
