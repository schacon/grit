## Task: t4217-log-limit

### Claim
- Claimed after completing `t4131-apply-fake-ancestor`.
- Marked as `[~]` in `PLAN.md`.

### Baseline
- `./scripts/run-tests.sh t4217-log-limit.sh` reports `1/3` passing (`2` left).

### Next
- Reproduce failing assertions in `tests/t4217-log-limit.sh`.
- Inspect revision filtering/limit behavior in `grit log` for mismatches.
- Implement and validate fixes until `3/3` passes.
