# t1510-repo-setup

- Extended `GIT_TRACE_SETUP` handling in `grit-lib/src/repo.rs` to match Git’s `setup:` lines (git_dir, common_dir, worktree, cwd, prefix) across discovery, `GIT_DIR`, gitfile, `core.worktree`, bare, and `GIT_WORK_TREE` combinations.
- Added discovery metadata (`discovery_root`, `discovery_via_gitfile`, `work_tree_from_env`), deduped `core.bare`/`core.worktree` warning, and relaxed `Repository::open` worktree canonicalization for non-existent paths (lexical join).
- Harness: `./scripts/run-tests.sh t1510-repo-setup.sh` → 109/109 pass.
