## Task: t4114-apply-typechange

### Claim
- Claimed after completing `t4054-diff-bogus-tree`.
- Marked as `[~]` in `PLAN.md`.

### Baseline
- Tracked harness:
  - `./scripts/run-tests.sh t4114-apply-typechange.sh`
  - result: **6/12 passing** (6 failing)

### Initial observations
- Current failures indicate `grit apply` typechange handling still diverges from upstream in several edge cases around mode transitions and path updates.

### Implementation
- Updated `grit apply` typechange handling in `grit/src/commands/apply.rs`:
  - treat symlink worktree paths as blob content equal to symlink target bytes for hunk preimage matching.
  - add robust path replacement helpers to remove/replace existing files, symlinks, and directories before writing target type.
  - apply mode-only cross-path rename/copy patches (no hunks) by materializing destination content from source-side preimage.
  - handle rename targets nested under source directories by deleting source tree before writing destination (`foo -> foo/baz`).
  - allow typechange sequences where a directory path is replaced by a file/symlink and subsequent deletions of prior descendants occur in the same patch stream.
  - keep symlink traversal guard active for unsafe writes while allowing valid in-stream directory→symlink replacement sequences.
- Updated `grit diff-tree` option parsing in `grit/src/commands/diff_tree.rs`:
  - accept `--binary` as a patch-mode-compatible flag so tests generating binary-aware patches proceed.

### Validation
- Targeted script:
  - `TRASH_DIRECTORY=/tmp/t4114-debug7 EDITOR=: VISUAL=: LC_ALL=C LANG=C GUST_BIN="/workspace/target/release/grit" bash t4114-apply-typechange.sh` (from `tests/`)
  - result: **12/12 passing**
- Tracked harness:
  - `./scripts/run-tests.sh t4114-apply-typechange.sh`
  - result: **12/12 passing**
- Quality gates:
  - `cargo fmt` ✅
  - `cargo clippy --fix --allow-dirty` ✅ (unrelated autofixes reverted)
  - `cargo test -p grit-lib --lib` ✅ (96/96)
